import { lazy } from 'react';

export const SorceressComponent = lazy(() => import('./Sorceress'));
export const MouseComponent = lazy(() => import('./Mouse'));
export const BenderComponent = lazy(() => import('./Bender'));
